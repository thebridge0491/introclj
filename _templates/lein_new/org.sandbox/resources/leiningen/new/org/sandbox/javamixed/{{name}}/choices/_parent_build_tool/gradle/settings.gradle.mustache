/*
 * This file was generated by the Gradle 'init' task.
 *
 * The settings file is used to specify which projects to include in your build.
 * 
 * Detailed information about configuring a multi-project build in Gradle can be found
 * in the user guide at https://docs.gradle.org/current/userguide/multi_project_builds.html
 */

startParameter.offline = true
//rootProject.buildFileName = 'build.gradle'
rootProject.name = '{{parent}}{{^parent}}intro_java{{/parent}}'

/* To declare projects as part of a multi-project build use the 'include' method
include 'shared'
include 'api', 'services:webservice'
*/
//include ":${rootProject.name}-util", ":${rootProject.name}-intro"
include ':bom', ':common', ':app'

project(':bom').name = "${rootProject.name}-bom"
//project(":${rootProject.name}-util").projectDir = file(':common')
project(':common').name = "${rootProject.name}-util"
project(':app').name = "${rootProject.name}-intro"
